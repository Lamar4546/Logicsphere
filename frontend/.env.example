# Create this file in frontend/.env
# Provide the demo org/user IDs used by the app while auth is not implemented.
VITE_DEMO_ORG_ID=your-org-uuid-here
# Browser-restricted Google Maps Embed API key. Enable Maps Embed API and restrict
# this key to your frontend origin before adding it here.
VITE_GOOGLE_MAPS_API_KEY=
